From 9a7a63647f9b34f9c0731166fd6d7014e723f4c3 Mon Sep 17 00:00:00 2001
From: Reyyzzz24 <revayuliansatria@gmail.com>
Date: Wed, 16 Sep 2026 14:31:27 +0700
Subject: [PATCH 17/25] feat(login): rewrite ResetPasswordForm with shadcn
 primitives (L3a)

@rocket.chat/layout Form*/ActionLink + Fuselage Field*/Button/Callout ->
local ui/input.tsx, ui/label.tsx, ui/button.tsx, FormCallout. register(),
handleSubmit, validation pattern, useSendForgotPassword, and all aria-*/
role attributes byte-identical to before.
---
 .../src/ResetPasswordForm.tsx                 | 101 +++++++++---------
 1 file changed, 48 insertions(+), 53 deletions(-)

diff --git a/packages/web-ui-registration/src/ResetPasswordForm.tsx b/packages/web-ui-registration/src/ResetPasswordForm.tsx
index 978e318..2a2f7a8 100644
--- a/packages/web-ui-registration/src/ResetPasswordForm.tsx
+++ b/packages/web-ui-registration/src/ResetPasswordForm.tsx
@@ -1,10 +1,12 @@
-import { FieldGroup, TextInput, Field, FieldLabel, FieldRow, FieldError, ButtonGroup, Button, Callout } from '@rocket.chat/fuselage';
-import { Form, FormContainer, FormFooter, FormHeader, FormTitle, ActionLink } from '@rocket.chat/layout';
 import { useDocumentTitle } from '@rocket.chat/ui-client';
 import { useEffect, useId, useRef } from 'react';
 import { useForm } from 'react-hook-form';
 import { useTranslation } from 'react-i18next';
 
+import { FormCallout } from './components/FormCallout';
+import { Button } from './components/ui/button';
+import { Input } from './components/ui/input';
+import { Label } from './components/ui/label';
 import type { DispatchLoginRouter } from './hooks/useLoginRouter';
 import { useSendForgotPassword } from './hooks/useSendForgotPassword';
 
@@ -14,7 +16,7 @@ export const ResetPasswordForm = ({ setLoginRoute }: ResetPasswordFormProps) =>
 	const { t } = useTranslation();
 	const emailId = useId();
 	const formLabelId = useId();
-	const forgotPasswordFormRef = useRef<HTMLElement>(null);
+	const forgotPasswordFormRef = useRef<HTMLFormElement>(null);
 
 	useDocumentTitle(t('registration.component.resetPassword'), false);
 
@@ -35,7 +37,7 @@ export const ResetPasswordForm = ({ setLoginRoute }: ResetPasswordFormProps) =>
 	const { mutateAsync, isSuccess } = useSendForgotPassword();
 
 	return (
-		<Form
+		<form
 			ref={forgotPasswordFormRef}
 			tabIndex={-1}
 			aria-labelledby={formLabelId}
@@ -43,63 +45,56 @@ export const ResetPasswordForm = ({ setLoginRoute }: ResetPasswordFormProps) =>
 			onSubmit={handleSubmit((data) => {
 				void mutateAsync({ email: data.email });
 			})}
+			className='ep-flex ep-flex-col ep-gap-6'
 		>
-			<FormHeader>
-				<FormTitle id={formLabelId}>{t('registration.component.resetPassword')}</FormTitle>
-			</FormHeader>
-			<FormContainer>
-				<FieldGroup>
-					<Field>
-						<FieldLabel required htmlFor={emailId}>
-							{t('registration.component.form.email')}
-						</FieldLabel>
-						<FieldRow>
-							<TextInput
-								{...register('email', {
-									required: t('Required_field', { field: t('registration.component.form.email') }),
-									pattern: {
-										value: /^[A-Z0-9._%+-]+@[A-Z0-9.-]+\.[A-Z]{2,}$/i,
-										message: t('registration.page.resetPassword.errors.invalidEmail'),
-									},
-								})}
-								error={errors.email?.message}
-								aria-invalid={Boolean(errors.email)}
-								aria-required='true'
-								aria-describedby={`${emailId}-error`}
-								placeholder={t('registration.component.form.emailPlaceholder')}
-								id={emailId}
-							/>
-						</FieldRow>
-						{errors.email && (
-							<FieldError aria-live='assertive' id={`${emailId}-error`}>
-								{errors.email.message}
-							</FieldError>
-						)}
-					</Field>
-				</FieldGroup>
+			<h1 id={formLabelId} className='ep-font-heading ep-text-h1 ep-text-font-default'>
+				{t('registration.component.resetPassword')}
+			</h1>
+			<div className='ep-flex ep-flex-col ep-gap-4'>
+				<div className='ep-flex ep-flex-col ep-gap-2'>
+					<Label htmlFor={emailId}>{t('registration.component.form.email')}</Label>
+					<Input
+						{...register('email', {
+							required: t('Required_field', { field: t('registration.component.form.email') }),
+							pattern: {
+								value: /^[A-Z0-9._%+-]+@[A-Z0-9.-]+\.[A-Z]{2,}$/i,
+								message: t('registration.page.resetPassword.errors.invalidEmail'),
+							},
+						})}
+						invalid={!!errors.email}
+						aria-invalid={Boolean(errors.email)}
+						aria-required='true'
+						aria-describedby={`${emailId}-error`}
+						placeholder={t('registration.component.form.emailPlaceholder')}
+						id={emailId}
+					/>
+					{errors.email && (
+						<p aria-live='assertive' role='alert' id={`${emailId}-error`} className='ep-text-c1 ep-text-red-600'>
+							{errors.email.message}
+						</p>
+					)}
+				</div>
 				{isSuccess && (
-					<FieldGroup>
-						<Callout aria-live='assertive' role='status' marginBlockStart={24} icon='mail'>
-							{t('registration.page.resetPassword.sent')}
-						</Callout>
-					</FieldGroup>
+					<FormCallout aria-live='assertive' role='status' type='info'>
+						{t('registration.page.resetPassword.sent')}
+					</FormCallout>
 				)}
-			</FormContainer>
-			<FormFooter>
-				<ButtonGroup>
-					<Button type='submit' loading={isSubmitting} primary>
-						{t('registration.page.resetPassword.sendInstructions')}
-					</Button>
-				</ButtonGroup>
-				<ActionLink
+			</div>
+			<div className='ep-flex ep-flex-col ep-gap-3'>
+				<Button size='lg' className='ep-w-full' disabled={isSubmitting} type='submit'>
+					{t('registration.page.resetPassword.sendInstructions')}
+				</Button>
+				<button
+					type='button'
+					className='ep-self-start ep-text-p1 ep-font-medium ep-text-brand-600 hover:ep-underline'
 					onClick={(): void => {
 						setLoginRoute('login');
 					}}
 				>
 					{t('registration.page.register.back')}
-				</ActionLink>
-			</FormFooter>
-		</Form>
+				</button>
+			</div>
+		</form>
 	);
 };
 
-- 
2.50.1 (Apple Git-155)

